name: flutter_golden_ratio_visualization
description: Discover the mesmerizing dance of the Golden Ratio in Flutter's animated visualization.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true